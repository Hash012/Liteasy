# Liteasy Phase 3 组织空间测试指南

这份文档面向没有软件开发经验的成员，用来验收 Phase 3“组织空间、组织治理、个人中心、设置左边栏和右栏助手边界”是否能在桌面端正常工作。

## 1. 测试前先确认

Phase 3 当前是可验收原型交付，不是正式组织协作后台。它主要验证产品方案原文中的这些入口和边界：

- `左边栏` 是最左侧窄竖栏，类似 VSCode Activity Bar，包含 `文献库 / 组织 / 个人中心 / 设置`
- `左栏` 是窄竖栏右侧的内容区域，会在文献库、组织、个人中心、设置之间切换
- `右栏` 只保留极简 AI 助手对话框，不再承载组织空间、组织治理、模型接入策略或文献元数据同步卡片
- 用户可以看到自己加入的组织、组织成员、通知、共享文献库、配额、任务和审计摘要
- 组织共享文献库必须由用户显式打开，行为类似 VSCode 打开新文件夹：切换当前工作区，而不是与本地文献库取并集
- 个人中心可以配置性别、年龄、学段，打开学术档案，并走清空画像确认 seam
- 右栏助手支持初始三模式入口、历史会话、新建会话、语音输入预留、命令路由和带原文定位的问答展示

完整边界请先看：

```text
/home/octopus/Liteasy/docs/qa/phase3-governance-limitations.md
```

## 2. 启动开发云服务

打开第一个终端，在仓库根目录执行：

```bash
cd /home/octopus/Liteasy
node /home/octopus/Liteasy/services/dev-cloud/server.mjs
```

看到下面这行表示成功：

```text
Liteasy dev cloud listening on http://127.0.0.1:8787
```

浏览器直接打开 `http://127.0.0.1:8787/` 时应看到开发云服务索引 JSON，而不是空白页或 `not_found`。这个地址只是开发云 API 服务，不是桌面端页面。

## 3. 启动桌面端

打开第二个终端执行：

```bash
cd /home/octopus/Liteasy/desktop
source "$HOME/.cargo/env"
npm run tauri dev
```

如果桌面窗口打不开，也可以先执行：

```bash
npm run dev
```

然后打开终端里显示的本地地址，一般是：

```text
http://127.0.0.1:1420/
```

## 4. 检查基础布局

启动后应能看到：

- 顶部品牌区带有微型模型指示，例如 `模型：云代理`
- 最左侧窄竖栏显示 `文献库 / 组织 / 个人中心 / 设置`
- 左栏默认是 `Library`，展示 `我的文献库`
- 中栏是 `Reader`
- 右栏是 `AI Assistant`，初始态显示 Liteasy 学术助手、三种模式入口、历史会话/新建会话按钮、输入框、语音预留和发送按钮

右栏不应出现 `模型接入策略`、`文献元数据同步`、`组织空间` 或 `组织治理` 卡片。

## 4A. 检查右栏 AI Assistant

在右栏初始态确认：

- 显示 `Liteasy 学术助手`
- 显示 `名词解释模式`、`命令模式`、`问答模式`
- 显示 `新建会话` 和 `历史会话`
- 点击 `语音` 后出现 `语音输入接口已预留，当前版本请先使用文本输入。`

在命令模式输入：

```text
同步云端策略
```

正常结果：右栏会追加命令反馈，设置页中的模型策略同步状态也能更新。你也可以在命令模式输入 `使用本地开发云端点`，系统会通过同一个安全 action 把云代理端点和控制平面端点恢复到 `http://127.0.0.1:8787`。点击 `新建会话` 后当前对话进入 `历史会话`，点击历史列表中的恢复按钮可以恢复会话。


## 5. 连接开发云账号

在 Liteasy 窗口顶部找到 `云账号` 卡片，点击：

```text
连接开发云账号
```

正常结果：

- 顶部显示 `Liteasy Researcher`
- 邮箱显示 `researcher@liteasy.dev`
- 账号状态提示会话已经保存在本地

## 6. 检查设置页

在最左侧窄竖栏点击：

```text
设置
```

左栏会切换为 `设置`。你应该能看到：

- `模型接入策略`
- `同步云端策略`
- `开发云端点诊断`
- 当前 `云代理端点` 和 `控制平面端点`
- `使用本地开发云端点` 按钮
- `文献元数据同步`
- `重新同步文献元数据` 按钮
- `当前通道：云代理` 或 `当前通道：本地直连`
- `同步状态：已同步`

本地验收时，`开发云端点诊断` 通常应显示 `http://127.0.0.1:8787`。如果看到 `mock://...` 或误填了 `http://127.0.0.1:1420`，点击 `使用本地开发云端点` 可把云代理端点和控制平面端点恢复到本地开发云 API。`http://127.0.0.1:1420` 是前端页面，不是开发云 API。

模型接入策略的详细切换和同步状态都在设置页；主页顶部只保留微型模型状态指示。

## 7. 检查组织页

在最左侧窄竖栏点击：

```text
组织
```

左栏会切换为 `组织`。连接账号后应该能看到：

- `组织空间：Liteasy AI Reading Lab`
- `已加入组织：Liteasy AI Reading Lab、Liteasy Literature Ops`
- `角色：研究员 · 成员 12 人`
- `组织成员：Liteasy Researcher（研究员）、Admin（管理员）`
- `共享文献库：组织共享文献库 · 48 篇`
- `通知：公告 · 管理员发布了本周阅读主题。`
- `通知：文献上传 · 成员上传了 Graph Neural Networks 综述。`
- `通知：文献库变更 · 共享文献库结构新增 RAG 目录。`
- `未读通知：3 条`

点击 `全部标记已读` 后，未读计数变为 `未读通知：0 条`，中栏提示显示 `组织通知已全部标记为已读。`。刷新或重新打开前端后，本机浏览器会从 localStorage 恢复已读状态；切换到其他组织时，通知已读状态按组织隔离，不会因为通知 id 相同而串读；点击 `断开云账号` 会清理本机通知已读状态。

组织页还包含 `打开组织窗口`，用于查看更完整的组织列表和详情。

点击 `创建组织` 会出现 `创建组织` 弹窗，默认组织名称为 `Liteasy Demo Organization`。当前为 demo seam：点击 `创建 demo 组织申请` 会在组织左边栏显示同文案的操作反馈，并同步更新中栏提示 `已创建 Liteasy Demo Organization 的 demo 组织申请，等待正式后端接入。`，不会写入真实组织列表、申请云端空间或触发计费；点击 `取消` 或 `关闭` 不会创建提示。

点击 `加入组织` 会出现 `加入组织` 弹窗，默认邀请码为 `LITEASY-DEMO-JOIN`。当前为 demo seam：点击 `提交 demo 加入申请` 会在组织左边栏显示同文案的操作反馈，并同步更新中栏提示 `已提交组织邀请码 LITEASY-DEMO-JOIN 的 demo 加入申请，等待正式后端接入。`，不会校验真实邀请码、写入成员关系或触发管理员审批；点击 `取消` 或 `关闭` 不会创建提示。

点击 `邀请成员` 会出现 `邀请成员确认` 弹窗。当前为 demo seam：点击 `发送 demo 邀请` 会在组织左边栏显示同文案的操作反馈，并同步更新中栏提示 `已创建 Liteasy AI Reading Lab 的 demo 邀请，等待正式后端接入。`，不会发送真实邮件；点击 `取消` 或 `关闭` 不会创建提示。

点击 `退出组织` 会出现 `退出组织确认` 弹窗。当前为 demo seam：点击 `创建 demo 退出请求` 会在组织左边栏显示同文案的操作反馈，并同步更新中栏提示 `已创建退出 Liteasy AI Reading Lab 的 demo 请求，等待正式后端接入。`，不会移除组织列表、成员关系或共享文献库访问；点击 `取消` 或 `关闭` 不会创建提示。

## 8. 切换已加入组织

在组织页中点击：

```text
查看 Liteasy Literature Ops
```

正常结果：

- 组织空间标题变为 `组织空间：Liteasy Literature Ops`
- 角色摘要变为 `角色：管理员 · 成员 4 人`
- 共享文献库摘要变为 `共享文献库：文献运营共享库 · 16 篇`
- 组织治理摘要变为 `治理后台：待复核 1 项，高风险 0 项`

当前只是两个固定 demo 组织之间切换，不代表已经支持真实组织加入、创建或退出。

## 9. 检查组织治理

在组织页查找 `组织治理` 卡片。你应该能看到：

- `治理后台：待复核 3 项，高风险 1 项`
- `组织配额：存储 38 / 100 GB，模型调用 4200 / 10000`
- `后台任务：组织共享文献库索引刷新（running）`
- `审计队列：Admin 更新共享文献库上传权限（medium）`

这些内容只是治理可视化入口，不代表已经有正式管理员后台。

## 10. 打开组织窗口

在组织页点击：

```text
打开组织窗口
```

正常结果：

- 页面中央出现 `组织窗口`
- 左侧显示组织列表
- 右侧显示选中组织的成员、通知和共享文献库详情
- 可以点击 `打开 Liteasy Literature Ops 详情` 查看第二个 demo 组织

## 11. 打开组织共享文献库

在组织页点击：

```text
打开共享文献库
```

正常结果：

- 左栏自动切回 `文献库`
- 左栏显示 `当前工作区：组织共享文献库（Liteasy AI Reading Lab）`
- 左栏 `我的文献库` 中出现 `Organization Reading List: Retrieval-Augmented Generation`
- 左栏 `我的文献库` 中出现 `Team Notes on Long-Context Evaluation`
- 本地 starter 文献不再显示在当前工作区中
- 中栏提示更新为 `已打开组织共享文献库：组织共享文献库。`

这一步的行为类似 VSCode 打开一个新的文件夹：切换当前工作区，而不是把组织文献和本地文献取并集。连接开发云账号和切换到组织页只加载组织信息，不会自动打开共享文献库。

如果 `打开共享文献库` 按钮不可点击，组织空间卡片下方会显示原因，例如共享文献库同步中、不可用或暂无可打开文献。

如果需要回到本地 demo 文献，在左栏点击：

```text
返回本地文献库
```

正常结果：

- 左栏显示 `当前工作区：本地文献库`
- `Attention Is All You Need` 和 `BERT: Pre-training of Deep Bidirectional Transformers` 重新出现
- 组织共享文献不再显示在当前工作区中
- 中栏提示更新为 `已返回本地文献库。`

也可以在右栏命令模式输入：

```text
打开组织共享文献库
```

正常结果与点击按钮相同。这个命令已经走注册 skill/action 边界，不是把自然语言直接映射成本地任意操作。

当前这一步只切换开发云返回的 demo 组织文献工作区，不代表已经支持真实云端目录浏览、上传或删除。

## 12. 打开个人中心

在最左侧窄竖栏点击：

```text
个人中心
```

正常结果：

- 左栏文献库区域切换为 `个人中心`
- 显示 `昵称：Liteasy Researcher`
- 显示 `用户 ID：demo-session-1`
- 显示 `所在团队：Liteasy AI Reading Lab`
- 显示 `画像配置：性别 未设置 · 年龄 未设置 · 学段 未设置`
- 可在 `画像配置表单` 中选择性别、填写年龄、选择学段，并点击 `保存画像配置`
- 显示 `用户画像：已关闭`

点击：

```text
开启用户画像
```

正常结果：

- 显示 `用户画像：已开启`
- 显示 `已阅读论文数：2`
- 显示 `学术人格：跨学科综述型`
- 显示 `学术档案` 和 `清空用户画像（需鉴权）` 按钮

继续点击 `学术档案` 应出现 `学术档案页面`，并显示当前画像配置；点击 `清空用户画像（需鉴权）` 后应出现 `清空用户画像确认`，确认后用户画像关闭，基础身份信息保留。

## 13. 当前不能验收什么

不要把下面能力当作已经完成：

- 真实组织创建、邀请、加入、退出
- 真实组织权限、管理员审批和风控策略
- 真实云端共享文献库目录浏览、上传、删除和版本管理
- 真实微信、飞书、本地文件授权采样
- 真实清空云端或本地用户画像数据


## 14. 自动化验收命令

如果你有开发环境，可以在仓库根目录执行：

```bash
cd /home/octopus/Liteasy/desktop
npm test
npm run build
cd /home/octopus/Liteasy
node --test services/dev-cloud/server.test.mjs services/dev-cloud/providers/openaiResponses.test.mjs
```

最近一次 Phase 3 收尾验证结果：桌面端 62 个测试文件 / 219 个测试通过，桌面生产构建通过，开发云 17 个测试通过。
