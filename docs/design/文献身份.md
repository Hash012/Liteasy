下面是当前工作区已经实现的论文身份架构。绿色是当前正式主链，黄色是兼容或风险路径，灰色是独立的开发云模型。

```mermaid
flowchart TB
  subgraph Desktop["Liteasy Desktop"]
    PDF["用户上传 PDF"]
    DOC["documentId<br/>逻辑文件副本"]
    HASH["contentHash<br/>PDF 字节去重/缓存"]
    PI["PaperIdentity 候选<br/>DOI > arXiv > S2 > OpenAlex<br/>> 题录指纹 > local ID"]
    SNAP["LiteratureRecord 本地快照"]
    PUB["引用 / 公开批注"]
    LEGACY["薄读旧同步判断<br/>只排除 local_paper_id"]
  end

  subgraph Resolver["Intuecho Literature Resolver"]
    RESOLVE["resolve<br/>内部库 + 外部来源"]
    EXACT["exact"]
    AMBIG["ambiguous<br/>用户选择"]
    NOTFOUND["not_found"]
    REFETCH["按 candidateKey<br/>服务端重新抓取"]
    MANUAL["manual 人工确认<br/>可仅用题名 + 作者 + 年份"]
  end

  subgraph Providers["当前 Provider"]
    CROSSREF["Crossref"]
    ARXIV["arXiv"]
    OPENALEX["OpenAlex"]
    S2["Semantic Scholar"]
  end

  subgraph IntuechoDB["Intuecho 独立数据库"]
    RECORD["literature_records<br/>一个内部 literatureId"]
    IDS["literature_identities<br/>多个身份别名"]
    TARGET["annotation_targets<br/>批注指向 literatureId"]
  end

  PDF --> DOC
  PDF --> HASH
  PDF --> PI

  PI --> RESOLVE
  RESOLVE <--> CROSSREF
  RESOLVE <--> ARXIV
  RESOLVE <--> OPENALEX
  RESOLVE <--> S2

  RESOLVE --> EXACT
  RESOLVE --> AMBIG
  RESOLVE --> NOTFOUND
  EXACT --> REFETCH
  AMBIG --> REFETCH
  NOTFOUND --> MANUAL

  REFETCH -->|public_registry| RECORD
  MANUAL -->|manual 也成为正式记录| RECORD
  RECORD --> IDS
  RECORD --> TARGET
  RECORD -.确认结果写回.- SNAP
  SNAP --> PUB

  PI --> LEGACY
  LEGACY -.题录指纹也可能进入旧同步.- TARGET

  classDef formal fill:#e8f5e9,stroke:#2e7d32,color:#172019;
  classDef warn fill:#fff4ce,stroke:#a15c00,color:#302000;
  classDef local fill:#e8f0fe,stroke:#3568a8,color:#172238;

  class RECORD,IDS,TARGET,REFETCH,SNAP,PUB formal;
  class MANUAL,LEGACY,NOTFOUND warn;
  class PDF,DOC,HASH,PI local;
```

当前正式数据模型是：

```mermaid
erDiagram
  LITERATURE_RECORDS ||--o{ LITERATURE_IDENTITIES : "拥有别名"
  LITERATURE_RECORDS ||--o{ ANNOTATION_TARGETS : "被批注引用"
  ANNOTATIONS ||--o{ ANNOTATION_TARGETS : "包含目标"

  LITERATURE_RECORDS {
    text id PK
    text title
    json authors
    int publication_year
    text record_source "legacy_metadata | manual | public_registry"
    text source_provider
    int revision
  }

  LITERATURE_IDENTITIES {
    text literature_id FK
    text identity_kind
    text identity_value UK
    text identity_source
  }

  ANNOTATION_TARGETS {
    text annotation_id FK
    text literature_id FK
  }
```

这里没有版本关系表。因此当前预印本和正式版只有两种结果：

```mermaid
flowchart LR
  PRE["arXiv 预印本"]
  PUB["DOI 正式版"]

  PRE -->|provider 把两个 ID 放在同一候选中| MERGED["一个 literatureId<br/>arXiv 和 DOI 都是别名"]
  PRE -->|来源没有共同稳定 ID| SPLIT1["预印本 literatureId"]
  PUB -->|来源没有共同稳定 ID| SPLIT2["正式版 literatureId"]

  SPLIT1 -.当前没有关系.- SPLIT2

  classDef risk fill:#fff4ce,stroke:#a15c00,color:#302000;
  class MERGED,SPLIT1,SPLIT2 risk;
```

此外还有一套独立的开发云模型：

```mermaid
flowchart LR
  CLIENT["客户端提交 identities"]
  ROUTE["development/dev-cloud<br/>POST /v1/works/resolve"]
  WORKS["works"]
  WIDS["work_identifiers<br/>verified / is_preprint_of"]
  GRAPH["citation_edges"]

  CLIENT --> ROUTE --> WORKS
  WORKS --> WIDS
  WORKS --> GRAPH

  FORMAL["Intuecho<br/>literature_records"]

  WORKS -.-|"数据库、ID 和确认策略均不相通"| FORMAL

  classDef dev fill:#eeeeee,stroke:#666,color:#222;
  class CLIENT,ROUTE,WORKS,WIDS,GRAPH dev;
```

与已确认新设计的核心差距集中在五处：

1. `manual` 目前也能创建正式可引用记录。
2. 四个 provider 目前没有权威源/聚合源等级，单个聚合源也能确认。
3. 正式模型没有 `confirmed/ambiguous/conflict` 持久状态。
4. 没有预印本与正式版关系表，结果可能被合并，也可能完全失联。
5. 旧题录指纹和薄读同步仍存在绕过正式 `LiteratureRecord` 的路径。

新设计的目标，就是把黄色路径收敛掉，并在正式 Intuecho 模型中增加分级确认和版本关系，而不是继续扩展 `dev-cloud works`。