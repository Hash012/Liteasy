文件系统改进：自定义保存目录、真实位置与 Liteasy Path 上下文

解决的问题
1. 业务数据原先分散依赖系统应用目录，用户无法统一选择保存位置。
2. 文献、笔记与生成产物缺少统一位置入口，难以区分独立文件、数据库内容和云端资源。
3. 内部资源地址不能直接成为 Agent 可读取的上下文，只有路径字符串不足以保证模型读取真实内容。

实现
- 设置增加数据保存位置、打开目录、待迁移目标、取消及重启迁移入口。
- Rust 统一业务数据根目录；启动后台服务和存储前完成复制与 SHA-256/目录清单校验，成功后切换指针，保留原目录。拒绝覆盖、目录互相包含、符号链接和 Windows 联接；配置目录不可用时不创建空库。
- 发布副本后指针写入中断可恢复，但必须重新验证标记和全部业务数据。旧目录变化时拒绝启用过期副本。保留历史目录别名，兼容 Windows 普通路径和规范路径。
- 迁移对象及附件、默认文献库、批注和产物、文件授权记录、聊天、Agent 状态、任务检查点与导出记录。外接 Vault、独立文献库、用户导出文件、WebView 配置、系统凭据及缓存保持各自存储边界。
- 文献、Notes、产物库和对象详情提供位置与 Liteasy Path 入口；本机支持文件管理器定位，数据库/浏览器/云端位置如实呈现。外部文件变化或删除时，旧对象快照的位置回退到对象数据库。
- Agent 支持路径输入及直接粘贴。解析并校验当前账号，读取实际正文，形成固定版本 contextRefs，经公共 Agent API 传入。支持对象、论文全文、论文与产物批注、产物及授权 Markdown/Canvas；兼容既有产物资源 URI 和摘要版本校验。
- 拒绝跨账号路径、目录跳转、非法参数及失效资源；论文准备完成前禁止发送；沿用上下文容量限制。
- 修复仅由笔记等对象生成、无来源论文的产物无法在原生端保存的问题。

验证
- 功能相关 Vitest 5 文件、34 项通过。
- cargo test --locked：90 项通过；最后补充 Windows 目录别名后迁移 3 项复测通过；cargo fmt 检查通过。
- npm run build 通过：TypeScript、schema 生成及 152 个运行资源校验通过；仍有现有大 chunk 提示。
- Chromium 浏览器端到端 1 项通过：真实 Markdown 导入、位置展示、复制路径、添加上下文并核对真实正文。
- 全量 Vitest 首轮 2520 项通过、4 项跳过、2 项失败。失败为既有 ArtifactLibraryPane 重命名和 PaperResourceTab 重译确认弹窗用例；分别独立复测 11/11、10/10 通过。没有将单独复测描述为全量首轮全绿。
- git diff --check 通过。

边界
- Windows 安装包的目录选择、重启迁移及 Explorer 定位尚需实机验收。
- 路径不授予额外权限；外部文件直接读取限现有服务支持的 Markdown/Canvas，其他格式需先导入受支持资源。
- 仅提交此次功能相关 38 个文件；工作区原有 CI 改造未包含在本提交中。
- 详细设计及验证见 docs/engineering/liteasy-path-and-data-location.md。
