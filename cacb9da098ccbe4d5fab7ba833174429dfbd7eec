解决的问题：PDF 摘录、AI 回答和研究笔记缺少统一的保存、引用、来源追踪及跨论文组织方式。

新增对象工作台，将内容保存为有稳定身份、不可变版本和来源锚点的对象；白板可复用同一对象，支持捕获、编辑、来源跳转及作为 Agent 上下文。桌面采用 SQLite、浏览器采用 IndexedDB，补齐账号隔离、事务保存和旧白板迁移。

边界：实现本地对象闭环，不代表云同步、自动规划或 Windows/macOS 原生交互已验收。
