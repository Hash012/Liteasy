# 薄读运行验收与人工开发接手

更新时间：2026-07-28

本文用于交接薄读模态的运行验收、真实模型链路验证和后续人工开发。需求优先级仍然是：`新模态-薄读.md`、用户对话补充、`薄读-完成标准.md`，最后才是当前代码实现。

## 当前结论

薄读已实现并自动验证了当前 `薄读-完成标准.md` 中的大部分核心链路，但尚不能宣称整体“很好地完成”。以下是已有工程能力，不等同于真实 PDF gold set、完整人工 UI/性能验收或 Intuecho 后端已经完成：

- 生成与继续深入都走真实 Agent 链路，不再用本地模板伪造薄读正文；薄读页面会直接显示当前 artifact task 的检索、生成、审计或保存阶段与进度，避免分支生成期间只显示空白或静态等待文案。
- 薄读始终是单篇阅读任务：当前阅读区已打开且被选中的论文会优先进入首位，root context、导入、Agent evidence scope 与保存的 artifact papers 都只保留这篇 `primaryPaperId`；其他已选论文不应混入全篇总述。继续深入时也会重新收敛旧缓存或异常文档中的 `paperIds` / artifact paper refs 到该主论文，不能让历史多篇数据扩大 branch evidence scope。多论文集合仍服务于其他分析模态。
- OpenAI 兼容链路和 DeepSeek 链路都已 live smoke 通过。
- 薄读产物、批注、pending_public 队列、prompt 侧重点都按 `artifactId` 作为边界。
- 首次薄读目标论文遵循“当前 PDF 阅读区打开且已选中优先，否则取选中第一篇”的规则。
- 论文身份链按 `doi -> arxivId -> semanticScholarId -> title+authors+year hash -> local paper id` 保存。
- 总述/下钻保留句子级证据映射、论文内 evidence span、claim 状态和外部知识边界；闭包外会改变视觉状态。
- 论文闭包外分支已并行接 OpenAlex 与 Crossref 真实检索，并按 DOI 去重重排；模型只能引用本轮返回的 source ID，外部上角标可打开论文来源 URL，来源区另提供不可替代的 provider record URL 用于核验上游记录。Crossref 来源以规范 DOI 和 `https://api.crossref.org/works/<encoded-doi>` 保存，且永远只能标为“主题检索命中”；目标论文引用、引用目标论文、相关工作三类关系仍只由 OpenAlex 图字段确定。
- live 模型输出经过严格句级 trace gate；不完整输出只允许一次有边界的结构修复，连续失败不保存产物。
- 薄读产物和 PDF 原文的私有批注、公开开关、自动公开、编辑、删除和“等待 Intuecho 同步”本地闭环已存在。PDF 原文批注会保存 paper identity、页码和文本层矩形范围，公开项使用独立的 `pdf_passage` scope，不会伪装成薄读 artifact。
- 中文 / English / 跟随系统的薄读生成语言设置已接入。

仍未完成的核心验收包括：扩展真实 PDF gold set、扫描版 PDF 的 OCR / 模糊匹配 / 稳定坐标定位、更多 provider（例如 Semantic Scholar）与真实联网多 provider 验收、Intuecho 真实后端联调、live-model nightly eval，以及跨视口 UI/性能人工验收。薄读与 PDF 原文都具备显式 HTTPS Intuecho 端点设置和手动同步入口：远端确认的 `intuechoAnnotationId` / `syncedAt` 才会保存为已同步；失败项保留错误与重试资格，编辑待同步项会重新进入队列。PDF 原文批注按 `paper.id + sourcePath` 保存在本地 WebView storage，可随同一文献重开恢复；旧版本地批注会迁移为私有记录。PDF 公开项的 payload 携带稳定 paper identity、页码和矩形范围，并在 `local_paper_id` 身份时拒绝联网。当前没有实际社区端点，不能将此称为后端同步已经验收。扫描 PDF 的 OCR 语言可在设置中选择 English、简体中文或中英混合，并会传入下一次导入的 Tesseract worker；OCR 证据仍保守地只支持页级定位。当前已实现的是 artifact 隔离的单轮 OpenAlex/Crossref 检索失败恢复与结果复用，不是无界 citation traversal。

## 运行入口

### OpenAI 兼容测试接口

推荐用于浏览器前端验收：

```bash
cd LiteasyClaw/desktop
npm run dev:test-api
```

该脚本读取仓库根目录 `project-docs/test-api.md` 中的 `OPENAI_KEY` 和 `API_END_POINT`，映射为 dev-cloud 所需的 `OPENAI_API_KEY` 和 `OPENAI_BASE_URL`。默认模型已改为当前实测可用的 `gpt-5.4-mini`。

注意：

- 不要把 `project-docs/test-api.md` 中的密钥复制到终端输出、截图、issue 或文档。
- 如果代理端点支持模型变化，优先用 `/v1/models` 查询可用模型，再设置 `VITE_LITEASY_OPENAI_MODEL`。

### DeepSeek

DeepSeek 通过 `LiteasyClaw/services/dev-cloud/.env.local` 配置：

```dotenv
DEEPSEEK_API_KEY=<本地密钥>
DEEPSEEK_BASE_URL=https://api.deepseek.com
LITEASY_MODEL_PROVIDER=deepseek
```

当前已确认一个实际问题：`.env.local` 中的 DeepSeek key 不能带额外单引号。此前 401 是由于 key 被 `'...'` 包住；去掉外层引号后，`deepseek-v4-flash` live smoke 通过。

### 普通开发

```bash
cd LiteasyClaw/desktop
npm run dev
```

普通开发模式可用于 UI 和本地工作流调试。薄读验收不能用 mock endpoint 作为成功依据；薄读请求设置了 `requireLive: true`，mock endpoint 应该报错并停止。

### Tauri

```bash
cd LiteasyClaw/desktop
npm run tauri dev
```

如果要在 Tauri 中验收真实模型，先把 dev-cloud `.env.local` 和 desktop `.env.local` 配好。不要把真实 key 写进已跟踪文件。

## 自动化验收

当前已在 2026-07-28 跑通：

```bash
cd LiteasyClaw/desktop
npm test
npm run build

cd ../services/dev-cloud
npm test
```

真实模型薄读（消耗模型调用，不纳入默认 `npm test`）使用：

```bash
cd LiteasyClaw/desktop
npm run test:thin-reading-live-model
```

该命令只从 `project-docs/test-api.md` 读取现有测试 API 配置，在可用 loopback 端口临时启动 `dev-cloud`，再执行桌面 `generateAssistantAnswer` 的真实薄读路径。它会验证 `execution.mode=live`、strict structured output、句级 trace gate、论文内 evidence span 和 summary sentence 映射；并以运行时实际生成的 evidence ID 对 ColBERT 的 late interaction / MaxSim / 效率取舍 root case 运行 gold 评分。ColBERT 的人工标注允许 `experimental` 或 `systems`，因为其主要贡献同时具有检索架构与实证方法特征；其他 gold case 默认仍要求单一明确类型。不会打印密钥或完整模型响应。运行完成会关闭临时服务。它仍是单一 root case，不替代跨论文的质量评测或人工阅读验收。

默认 provider 是 OpenAI。要让同一 quality gate 使用本地 `dev-cloud/.env.local` 中的 DeepSeek 配置，显式执行：

```bash
cd LiteasyClaw/desktop
LITEASY_THIN_READING_LIVE_PROVIDER=deepseek npm run test:thin-reading-live-model
```

该分支不会读取 `project-docs/test-api.md`；若 `DEEPSEEK_API_KEY` 未配置，会清晰失败，不会退回 mock。

当前 live gold case 默认是 `colbert`。可用 `LITEASY_THIN_READING_LIVE_EVAL_CASE=bert` 切换为 BERT 的双向预训练 root case，例如：

```bash
LITEASY_THIN_READING_LIVE_EVAL_CASE=bert npm run test:thin-reading-live-model
```

`LITEASY_THIN_READING_LIVE_EVAL_CASE=bert-external` 是单独的联网闭包外 case：它以 BERT DOI 请求 OpenAlex，并要求生成结果保留本轮实际返回的 relation、外部 source ID、HTTPS 论文链接、规范 `sourceRecordUrl` 和“论文外证据”边界。它不会把开放检索结果强行要求为特定论文或 `cites_target`，以免把合法的 `related` / `topic_search` 误判为失败；关系保真按每个 `summarySentences.externalKnowledge` 所绑定的 source 判定，不能因本轮另有 citation graph source 就把该 `topic_search` / `related` 来源写成 citation。违规输出会被 generation gate 拒绝并仅允许一次定向修复。AlphaFold `cites_target` 的具体图边由 `npm run test:thin-reading-openalex-live` 单独核对。它消耗一次外部检索和一次模型调用，不纳入默认测试。

`LITEASY_THIN_READING_LIVE_EVAL_CASE=bert-zh-branch` 会运行中文论文内下钻：它以 BERT 的 `masked language modeling（掩码语言建模）` 为当前页 evidence，并向模型传入上一层“双向预训练” claim/span。评测要求保留紧邻术语对、父层连续性、句级 evidence 映射和论文内闭包；它消耗一次模型调用，不纳入默认测试。

`LITEASY_THIN_READING_LIVE_EVAL_CASE=glue-benchmark` 会运行 GLUE 的基准评测型 root case：它使用仓库内 GLUE PDF 的九项 NLU 任务、资源比较与 linguistic diagnostic suite evidence，要求总述保留任务结构、句子/句对边界、跨设置可比较性与诊断层的作用，不能退化成泛化的数据集目录。它消耗一次模型调用，不纳入默认测试。

`LITEASY_THIN_READING_LIVE_EVAL_CASE=squad-dataset` 会运行 SQuAD 的数据集/资源型 root case：它使用仓库内 SQuAD PDF 的 crowdworker、Wikipedia passage 与 extractive answer evidence，要求总述保留资源构建、答案边界和评测用途，不能退化成模型结果摘要。它消耗一次模型调用，不纳入默认测试。

结果：

- Desktop Vitest：149 个测试文件、785 个测试全部通过；包含薄读 provider structured-output、关系/身份/trace/UI 聚焦回归。
- Desktop build：`tsc && vite build` 通过。
- dev-cloud：73 个 Node tests 全部通过。
- 已知非阻塞警告：AppShell 相关测试仍输出既有 React `act(...)` warning；Vite build 仍提示部分 chunk 超过 500 kB。

薄读重点测试文件：

- `src/tests/thinReadingAgent.test.ts`
- `src/tests/thinReadingExternalKnowledgeClient.test.ts`
- `src/tests/thinReadingProjection.test.ts`
- `src/tests/thinReadingTab.test.tsx`
- `src/tests/thinReadingIntuechoSyncQueue.test.ts`
- `src/tests/paperIdentity.test.ts`
- `src/tests/useArtifactActions.test.ts`
- `src/tests/generateAssistantAnswer.test.ts`
- `src/tests/runAgentArtifactAnalysis.test.ts`
- `src/tests/agentRequestScope.test.ts`
- `src/tests/artifactResultClient.test.ts`
- `src/tests/ArtifactTabs.test.tsx`
- `src/tests/AppShell.test.tsx`
- `src/tests/devScript.test.ts`

## Live Smoke 记录

已做过不打印密钥的真实链路 smoke：

- OpenAI 兼容代理：`/v1/models` 可返回模型列表，`gpt-5.4-mini` 生成 `LIVE_SMOKE_OK` 成功，`execution.mode=live`。
- DeepSeek：`deepseek-v4-flash` 生成 `LIVE_SMOKE_OK` 成功，`execution.mode=live`。
- OpenAlex：`POST /v1/research/external-knowledge` 以 ColBERT DOI `10.1145/3397271.3401075` 和实时查询运行成功。服务在搜索页缺少目标 work 时按 DOI 或 arXiv ID 精确补取一次目标；ColBERTv2 返回 `cites_target`，无图关系且无词汇相关性的 SciPy 噪声被过滤。smoke 未使用静态 fixture。
- 薄读真实质量门：2026-07-28 通过 `npm run test:thin-reading-live-model`（OpenAI 兼容测试 API）及 `LITEASY_THIN_READING_LIVE_PROVIDER=deepseek npm run test:thin-reading-live-model`（本地 DeepSeek 配置）的 ColBERT root case；也通过 `LITEASY_THIN_READING_LIVE_EVAL_CASE=bert npm run test:thin-reading-live-model` 的 BERT root case、`LITEASY_THIN_READING_LIVE_EVAL_CASE=bert-external npm run test:thin-reading-live-model` 的真实闭包外 OpenAlex case、`LITEASY_THIN_READING_LIVE_EVAL_CASE=colbert-zh npm run test:thin-reading-live-model` 的中文术语保真 case、`LITEASY_THIN_READING_LIVE_EVAL_CASE=bert-zh-branch npm run test:thin-reading-live-model` 中文 BERT 下钻 case、`LITEASY_THIN_READING_LIVE_EVAL_CASE=glue-benchmark npm run test:thin-reading-live-model` 的 GLUE 基准评测型 root case，以及 `LITEASY_THIN_READING_LIVE_EVAL_CASE=squad-dataset npm run test:thin-reading-live-model` 的 SQuAD 数据集/资源型 root case。BERT 中文下钻要求 `masked language modeling（掩码语言建模）` 承接父层“双向预训练”；GLUE case 要求保留九项 NLU 任务、句子/句对结构和可比较性，不得退化成泛化数据集目录；SQuAD case 要求保留 crowdworker、Wikipedia passage 与 extractive answer 的资源构建边界，不能写成模型结果摘要。三个 case 都同时满足论文内闭包、句级 trace 与已知 evidence span；OpenAI 兼容链路和 DeepSeek 均已通过。均经桌面 `generateAssistantAnswer`、临时 `dev-cloud`，验证 live trace、structured output、sentence trace、已知 PDF evidence span、外部来源可追溯性与关系保真。已覆盖四个论文根节点、一个中文下钻和两条 provider 链路，但不能外推为多论文整体质量结论。

验收时只记录 provider、model、`execution.mode`、是否包含 marker、耗时和错误类型。不要记录 key 或完整上游响应。

## 手工 UI 验收清单

1. 启动真实模型链路：OpenAI 用 `npm run dev:test-api`；DeepSeek 用 `.env.local` 切到 `LITEASY_MODEL_PROVIDER=deepseek` 后启动。
2. 导入或打开至少一篇 PDF，并确保目标论文处于选中状态。
3. 如果阅读区当前打开的 PDF 也在选中集中，点击薄读时应优先使用这篇；否则使用选中文献列表第一篇。
4. 点击多模态悬浮按钮中的“薄读”，观察 Agent 阶段反馈：检索证据、生成、审计、结构化产物保存。继续深入时，这些阶段和进度条应保留在薄读页面内，不能退化成无进度的等待状态。
5. 生成完成后检查页面：同一 Tab 内出现总述、层级、论文类型、遗漏板块按钮、Intuecho 推荐栏；`关键判断 / 论文内证据 / 外部知识` 应收敛在轻量“依据”区域中，不应压过总述主视觉。
6. 检查总述质量：不是平均章节摘要，而是按论文类型抓核心结论、关键推导和学科位置；中文默认应保留关键英文术语并中文括注。
7. 检查总述每个内容性句子都有证据标记；点击有论文内证据的句子，应打开对应 PDF 页并高亮“证据句”。展开“依据”后点击 evidence span，也应进入同一套 PDF quote 高亮路径；匹配失败可退回页级定位。
   自动化质量门还应证明 `summarySentences` 覆盖至少 95% 的正文、句级来源属于顶层白名单、`grounded` 只用于论文内 evidence；不能以 parser 猜测映射冒充模型输出完整。
8. 点击遗漏板块，薄读应在同一 Tab 进入下一层；如果已有同 source child，应复用旧 child，不重复生成。
9. 选中总述或下钻正文中的文字，弹出“深入/批注”入口；填写可选提示词后继续深入，应生成与选区和上一层 claim/evidence 连贯的内容。
10. 当父层已在闭包外，或选区提示明确要求外部/后续/相关工作时，应先看到外部检索阶段；生成后页面进入闭包外视觉状态，“外1”上角标应能打开真实来源 URL。悬停应显示目标论文引用、引用目标论文、相关工作或主题检索命中；搜索页、DOI 和 arXiv 精确解析都无法锁定目标 work 时只能显示主题检索命中。同一 `artifactId + query + target identity` 首次失败会保留失败状态，重新请求或 dev-cloud 重启后均可恢复；首次成功后复用已验证来源，不能生成假外部内容。
11. 对总述、claim、evidence、external knowledge、recommendation 添加批注；私有批注应保存、恢复、编辑、删除。
12. 勾选公开或开启自动公开后，批注应进入 `pending_public`，UI 显示“等待 Intuecho 同步”，不能显示成已公开。
13. 收起 Intuecho 推荐栏，应变为窄栏入口并保留 pending 数；再次点击可恢复。
14. 切换薄读生成语言为 English / 跟随系统，重新生成，检查 prompt 和 UI 后处理遵循目标语言。
15. 故意切到 mock endpoint 或错误 provider/key，薄读应停在错误态，不能输出本地假内容。

## 关键代码地图

- 薄读类型：`LiteasyClaw/desktop/src/app/features/thin-reading/thinReading.types.ts`
- Agent prompt / JSON 解析：`LiteasyClaw/desktop/src/app/features/thin-reading/thinReadingAgent.ts`
- 论文分类与 prompt registry：`LiteasyClaw/desktop/src/app/features/thin-reading/thinReadingPromptRegistry.ts`
- 文档投影、分支、批注：`LiteasyClaw/desktop/src/app/features/thin-reading/thinReadingProjection.ts`
- Intuecho pending 队列：`LiteasyClaw/desktop/src/app/features/thin-reading/thinReadingIntuechoSyncQueue.ts`
- UI：`LiteasyClaw/desktop/src/app/features/thin-reading/ThinReadingTab.tsx`
- UI 样式：`LiteasyClaw/desktop/src/app/features/thin-reading/thinReading.css`
- 文献身份链：`LiteasyClaw/desktop/src/app/features/paper-identity/paperIdentity.ts`
- artifact 编排：`LiteasyClaw/desktop/src/app/features/artifacts/useArtifactActions.ts`
- Agent request scope：`LiteasyClaw/desktop/src/app/controllers/agent/agentRequestScope.ts`
- Agent artifact 分析：`LiteasyClaw/desktop/src/app/controllers/agent/runAgentArtifactAnalysis.ts`
- 模型生成入口：`LiteasyClaw/desktop/src/app/features/assistant/generateAssistantAnswer.ts`
- 外部检索客户端：`LiteasyClaw/desktop/src/app/features/thin-reading/thinReadingExternalKnowledgeClient.ts`
- OpenAlex / Crossref adapter / endpoint：`LiteasyClaw/services/dev-cloud/payloads/externalKnowledgePayloads.mjs`、`requestHandler.mjs`、`db/externalKnowledgeRunRepository.mjs`
- 模型策略 / live-only：`LiteasyClaw/desktop/src/app/features/models/modelPolicy.ts`、`modelRuntime.ts`、`modelGateway.ts`、`modelHttpClient.ts`
- dev-cloud provider：`LiteasyClaw/services/dev-cloud/payloads/modelPayloads.mjs`、`providers/openaiResponses.mjs`、`providers/deepseekChatCompletions.mjs`
- 测试 API 启动脚本：`LiteasyClaw/desktop/scripts/dev-with-test-api.mjs`

## 当前数据契约

`ThinReadingDocument` 是薄读保存边界，必须按 `artifactId` 管理。`paperIdentity` 只用于跨 artifact 识别同一篇论文，不得替代 `artifactId`。

核心节点字段：

- `summary`: 当前层薄读文本。
- `paperType`: 论文类型。
- `withinPaperClosure`: 是否仍在论文闭包内。
- `evidence.paperEvidenceSpans`: 论文内 evidence span，含 `paperId`、`chunkId`、`page`、`quote`、`confidence`；可选 `pageTextStart` / `pageTextEnd` 是页内的规范化文本搜索坐标，用于 PDF text-layer 的重复 quote 消歧，不是原始 PDF 字节偏移。规范化规则固定为 NFKC、Unicode dash 统一为 `-`、删除 soft hyphen、折叠空白并转小写；提取器和阅读器共享同一规则，不能各自演化。
- `evidence.claims`: summary 关键判断，状态为 `grounded` / `weak` / `unsupported`。
- `evidence.summarySentences`: summary 每个内容性句子的证据边界，含 `text`、`evidenceIds`、`externalKnowledge`、`status`。
- `evidence.externalKnowledge`: 本轮已验证的 external source ID 列表，不是自由概念文本。
- `evidence.externalSources`: OpenAlex 结构化来源，保留 provider、source ID、DOI、论文落地 URL、不可变的 `sourceRecordUrl`、作者、年份、摘要、查询、相关度和 `relation`。desktop client、Agent attachment 和 artifact cache 都要求 `sourceId` 是 `W...`、`id` 严格等于 `openalex:<sourceId>`、`sourceRecordUrl` 严格等于 `https://openalex.org/<sourceId>`，落地 `url` 必须是 HTTPS；不能因落地页优先而丢失或伪造上游记录。`relation` 只允许 `cited_by_target`、`cites_target`、`related`、`topic_search`。DOI 或 arXiv 身份可触发一次精确目标解析；其他无法锁定目标 work 的情况保守使用 `topic_search`。
- `omittedSections`: 当前总述未覆盖但可深入的小标题按钮。
- `recommendationScope`: Intuecho 推荐和 pending 队列请求范围。
- `annotations`: 私有和 `pending_public` 批注。

本地 artifact cache 在恢复薄读前会校验 document 版本、`artifactId` 一致性、根/活动节点、批注队列、语言字段和外部来源载荷；不完整或错配缓存会被丢弃，不能覆盖可用产物。批注必须属于当前 artifact 和存在的节点，target 必须与该节点匹配，`pending_public` ID 只能指向对应可公开批注。节点 source 与 recommendationScope 也必须匹配：root 只能对应 whole-paper，遗漏板块必须保留同一 sectionKey，正文选区必须保留同一 excerpt 和 evidenceIds。旧版 OpenAlex 来源若仅缺少可由 `sourceId` 确定的 `sourceRecordUrl`，恢复时会安全补成 `https://openalex.org/<sourceId>`；其他不可信来源记录不迁移、不渲染。

旧产物若缺少 `summarySentences` 句级映射，界面会将每个摘要句显示为“待核”，不会从 node-level claim、`paperEvidence` 或外部来源猜测句级 grounded/weak 标记，也不会提供伪造的证据跳转；只有保存了明确句级映射的内容才能显示可点击上角标。同一句同时关联论文 evidence 和 external source 时，两类上角标必须并列显示为“证 n”和“外 n”，不得用其中一类遮蔽另一类来源。

页面选区保存批注时会保留内容 target：总述为 `node_summary`，外部来源为 `external_knowledge` 并带 source ID，Intuecho 推荐为 `recommendation` 并带 recommendation ID。不要在后续同步或恢复中把这些局部关联降级成无类型文本。选区批注在用户未显式勾选公开时也必须遵守 document 的 `autoPublic` 设置；开启自动公开后，所有批注入口都应进入本地 `pending_public` 队列。

从总述选区继续深入时，`selected_text` source 还会保存该选区相交的 `summarySentences.evidenceIds`。下一轮 prompt 将这些 ID 作为优先解释的父层证据，用于说明选区为什么值得继续读；它们不替代下一轮检索，下一层输出仍必须引用本轮 evidence ID。该分支的 `recommendationScope.kind="selected_passage"` 也会持久化同一组 `evidenceIds` 和 excerpt，并原样进入 `pending_public` 队列；未来 Intuecho 同步器必须用它们关联具体句段，不能把范围退化成整篇论文。恢复缓存时这些 ID 必须唯一，且都属于父节点的论文内 evidence 边界；不满足时整个薄读缓存会被拒绝，避免把伪造 ID 送入下一轮 Agent。

选区原文在进入 Agent prompt 时最多保留 1,600 字符，补充提示最多 600 字符；超出部分以省略号截断，防止长选区挤占本轮 evidence、JSON contract 与审计约束。页面补充输入使用同一 600 字符上限。

模型输出必须是可解析 JSON；错误 JSON、空 evidence、伪造 evidence ID 都应失败。论文原文、证据矩阵、父层文本、用户选区/补充资料和外部来源标题/摘要全部是不可信参考数据，不能覆盖薄读任务、结构化 schema 或 evidence/source 白名单；其中任何要求忽略规则、改变角色或泄露内容的文字都必须视为资料而非指令。
live 输出还必须通过 explicit trace gate。首次失败会进入 `repairing_structured_output`，修复 prompt 只能使用原 evidence/source 白名单；`thinReading.qualityGate` 保存 attempts、repaired 和 repairReasons。

## 后续人工开发优先级

### P0：质量评测闭环

建立 5-20 篇论文 gold set，至少覆盖实验型、方法型、系统型、数据集型、综述型和理论/人文型。每篇保存 gold summary、关键遗漏板块、选区下钻答案、证据位置和外部边界判定。

完成标准：

- 有可重复运行的 eval harness。当前可用命令为 `cd LiteasyClaw/desktop && npm run test:thin-reading-eval`。除 9 个手工参考场景外，已加入 8 篇仓库内公开 PDF（ColBERT、ACORN、Vector DBMS survey、Transformer、BERT、GLUE、SQuAD、古代语言语料分析）的 PDF-backed gold case；每条保存 SHA-256、相对路径、页码和 quote，并在测试中重新提取原始 PDF 核对。`npm run test:thin-reading-openalex-live` 是单独的 OpenAlex 联网图关系验收；`npm run test:thin-reading-external-live` 会同时核对 Crossref 的 AlphaFold DOI 规范记录、OpenAlex 图边，以及服务端聚合器是否返回 OpenAlex 和只能标为 `topic_search` 的 Crossref 来源，两者均不纳入默认测试。
- 评测记录 root summary 核心命中率、citation precision、citation recall、evidence grounding、branch relevance、external boundary、external source traceability、external relation fidelity、language consistency、unsupported claim 占比。citation precision 防止无关 evidence 混入，citation recall 要求每条 gold 必要 evidence 都实际被 candidate 的 summary/claim/paperEvidence 引用，不能因只引用部分正确证据而通过。`evidence grounding` 要求候选 span 的 evidence ID、页码和 quote 同时回溯到 gold PDF；闭包外的每个 externalKnowledge ID 必须能映射到本轮 provider 来源的上游 ID、HTTPS 论文链接、规范 `sourceRecordUrl`、关系标签与检索 query，不能用自由文本冒充来源。关系保真以句子映射到的具体 source 为准：Crossref 必须是 `topic_search`，`topic_search` / `related` 不得因同轮其他来源存在图边而被写成已验证 citation；明确的否定性边界说明不算误述。
- 真实 PDF-backed case 已达到 12 条，覆盖 ColBERT、ACORN、Vector DBMS Survey、Transformer、BERT、GLUE、SQuAD 和古代语言语料分析；其中有中文 ColBERT root case，以同一真实 PDF span 验证 `late interaction（后期交互）` 的紧邻术语对。新增中文 BERT 下钻 case 以第 2 页 MLM 原文验证 `masked language modeling（掩码语言建模）` 必须承接父层“双向预训练”，不能退化为中文改写或独立摘要。另含一条 BERT 英文下钻 case，验证选区机制与上一层主轴的承接，以及一条 GLUE diagnostic-suite 下钻 case，验证 benchmark 总述向语言学诊断目的的承接；还包括一条 GLUE benchmark case、一条 SQuAD 资源型 case，以及一条人文型 case。人文 case 锚定了 patristic literature 中 biblical reuse 的语文学研究对象，避免把计算方法压过研究问题。手工 gold suite 另有一个真实外部边界 case：2026-07-28 查询 OpenAlex 得到 AlphaFold (`W3177828909`) 引用 BERT (`W2963341956`)，因此只能作为 `cites_target` 的论文外图关系呈现，不能倒灌为 BERT 论文内证据。`npm run test:thin-reading-openalex-live` 会从 AlphaFold 的 `referenced_works` 重新核对这条边，且会在 work ID 或标题漂移时失败。后续仍需扩到 5-20 篇的完整跨类型集；新增 PDF 必须沿用同一 provenance 契约，不能用摘要文本替代原文证据。

### P0：人工完善 prompt registry

扩展 `thinReadingPromptRegistry.ts`，加入人工样例、类型冲突裁决、少样例提示词和语言风格约束。重点不是增加模板，而是让不同论文类型的“读完后真正留下的东西”被正确取舍。

完成标准：

- 每类论文至少 3 个人工案例。当前 9 类均已有 3 个中英文“信号 -> 留存”抽象案例，并且只注入当前候选类型；每类另有不输出给用户的 coverage audit，用于检查高价值阅读维度，避免把 Literature-Review-Assistant 式固定栏目直接变成薄读大纲。
- root / omitted_section / selected_text 三个 stage 都有差异化指导。当前已分别约束核心取舍、遗漏板块对父结论的作用、选区为何值得深入。
- 中英文输出有独立风格检查。当前 prompt 与 deterministic eval 已分语言处理；中文关键术语除了 gold eval 同时核对原词与释义外，live parser 还会用本轮 evidence terms 拒绝“中文（原文术语）”的反向括注。中文选区或补充提示明确给出“原文（中文释义）”时，生成入口会把该术语对作为本轮硬约束，遗漏后只允许一次有边界的修复。`bert-zh-branch` 已在 OpenAI 兼容和 DeepSeek 真实路径通过；仍需更多真实模型样本做风格人工评分。
- 类型冲突不能静默靠数组顺序决定。当前分类结果会记录 title/evidence 分项、runner-up、conflict 和 confidence，并在 prompt 中要求按主要贡献裁决。

### P1：证据定位升级

PDF text offset 已写入 retrieval chunk、analysis evidence 和 thin-reading evidence span，并在薄读 marker、ArtifactTabs evidence-open request、AppShell reader target 和 PDF 跳转中以 `pageTextStart` / `pageTextEnd` 保留。提取器与 PDF text layer 使用同一套 NFKC / dash / soft-hyphen / 空白规范化坐标；重复 quote 按离持久化 offset 最近的候选消歧，已覆盖“第二次出现的引文含 soft hyphen 和全角字符”的回归。精确匹配失败后还会做一次仅移除空白/连字符的紧凑匹配，以处理常见的跨行断词和 Unicode dash 漂移，命中后仍从真实 text-layer 建立高亮范围。无法匹配时，主阅读区会明确显示“页级定位”原因。扫描版或缺失文字层的 PDF 现在会在桌面端按页渲染后使用 Tesseract.js OCR；OCR worker 对同一文档复用，默认识别 `eng`。OCR 得到的 chunk、analysis evidence 和 thin-reading span 均标为 `textExtraction="ocr"`，阅读器会明确提示“该证据来自 OCR 识别，当前只能页级定位”，不会伪造原生 text-layer 高亮。2026-07-28 已用 Tesseract.js 自带图片完成 worker 与 `eng` 语言包 smoke，但 OCR 语言选择、真实扫描件的识别率和可稳定持久化的 glyph/DOM 坐标仍待验收。

完成标准：

- evidence span 已携带页内字符范围，并用于重复 quote 消歧。
- 精确匹配失败时已显示页级 fallback 原因。
- 扫描 PDF 会尝试实际 OCR，且 OCR 证据在 UI 中保守降级为页级定位；不能把 OCR 文本误报为可精确 text-layer 高亮。
- claim 到 evidence 的跳转和高亮仍需在真实 PDF、扫描 PDF 和跨缩放条件下完成稳定性验收。

### P1：结构化输出能力

薄读现在会把与客户端 Zod parser 对应的 JSON Schema 一并传到 provider：OpenAI Responses 使用 `text.format=json_schema` 且 `strict=true`，DeepSeek Chat Completions 使用 `response_format=json_object`。客户端仍以 Zod parser、evidence/source 白名单与 live trace gate 作最终裁判。兼容代理若以 `400` / `422` 拒绝结构化参数，provider 只会重试一次不带结构化参数的同一 JSON prompt；该兼容路径同样必须通过严格 parser 和 trace gate，绝不降级成假 seed。

完成标准：

- OpenAI / DeepSeek 的 provider 请求形状已有自动化覆盖。2026-07-28 已通过本地真实 dev-cloud 做最小 strict-schema smoke：OpenAI 兼容链路与 DeepSeek 都返回 `{"marker":"LIVE_SCHEMA_OK"}`，且 `execution.mode=live`。每次切换兼容代理、模型或 provider 配置后仍需重跑。
- provider 返回 schema 违规时由 Zod 和 trace gate 失败并显示可读错误；不得自动补出看似成功的假 seed。
- DeepSeek JSON-object 模式与后续 shape drift 的严格 parser 测试已覆盖；仍需累积真实模型失败样本。

### P1：外部知识检索扩展（OpenAlex + Crossref 第一阶段已完成）

当前已接 OpenAlex search 与 Crossref DOI 检索，按 DOI 去重重排，并实现 source ID 白名单、超时/空结果/上游失败分类、生成前停止和 UI 来源跳转。Crossref 只产生 `topic_search`，不能伪造成 citation graph；OpenAlex 是当前唯一能产生 cited / citing / related 的 provider。下一步接 Semantic Scholar，并进行真实联网的多 provider 验收。

完成标准：

- 外部知识引用不混入论文内 evidence。
- UI 能解释为什么越过闭包。当前闭包外节点会显示“已越出论文原文闭包”的原因，并列出本轮外部来源及 `cited_by_target` / `cites_target` / `related` / `topic_search` 的可读关系；上角标仍可直达原始记录。
- cited / citing / related works 至少有一条可追踪链路。

### P1：Intuecho 后端联调

薄读 artifact 和 PDF 原文 `pdf_passage` 都已有本地 pending queue 与 HTTPS sync adapter。不要把本地 pending 显示成已公开；只有远端返回 Intuecho annotation id 后才能标记 synced。下一步是与真实社区的认证、批量协议和 receipt 语义联调。

完成标准：

- 薄读队列按 `artifactId` 隔离；PDF 原文队列按 paper identity 和 annotation ID 隔离。
- 同步失败可重试且不阻塞其他 artifact。
- 同步 payload 使用 paper identity，而不是只用 local paper id。
- 真实 HTTP 同步器拒绝 `local_paper_id` 或缺失身份的条目，不发出网络请求；条目保持本地 `pending_public`，待补全 DOI、arXiv、Semantic Scholar 或题名作者年份身份后重试。

### P2：元数据抽取与身份链增强

把 PDF metadata、DOI/arXiv/Semantic Scholar 查询、云端 metadata sync 统一进 `paperIdentity.ts` 的输入链。

完成标准：

- DOI / arXiv / Semantic Scholar / authors / year 能从真实文献库补齐。
- fallback 到 local paper id 时 UI 和日志明确说明。
- 跨 artifact 同论文关联稳定。

### P2：UI 与性能打磨

继续优化薄读页面的加载态、分支导航、Intuecho 折叠、选区弹层和长文本布局。深层页面已显示完整祖先面包屑，且可直接回到任一祖先节点；大文献下仍要避免空白等待和 UI 跳版。

完成标准：

- 生成阶段始终有可见进度。
- 分支切换、完整祖先路径跳转、批注编辑、推荐栏开合不阻塞阅读。
- 移动和桌面视口无文字遮挡。

## 接手注意事项

- 不要提交 `.env.local`、`project-docs/test-api.md` 密钥内容、上游响应全文或任何 API key。
- 不要把 `pending_public` 改名成 `public`，直到 Intuecho 后端真的接通。
- 不要把同一篇论文的 paper identity 当作薄读产物边界；薄读边界始终是 `artifactId`。
- 不要把上一层 evidence span 当作下一层新证据；下一层输出仍必须引用本轮 evidence ID。
- 不要用 mock endpoint 验收薄读质量；mock endpoint 对薄读应失败。
- 当前 docs 里的“已通过”以 2026-07-28 的本地验证为准；换机器或换 key 后需要重跑 live smoke。
