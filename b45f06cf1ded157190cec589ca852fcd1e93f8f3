解决的问题：导入 PDF 后缺少自动获取可靠题录及按书目信息命名文件的流程，用户需要手工识别标题、作者和年份。

新增独立元数据识别队列和右键“获取元数据”入口，从首页证据、DOI/arXiv 标识检索并核对题录，确认后持久化元数据，按作者、年份、标题重命名托管 PDF。保留稳定文献 ID，处理 Windows 文件名限制、重名及陈旧查询结果，避免覆盖文件或用户修改；识别失败不否定已完成的全文索引。

边界：采用保守匹配，存在歧义时保留原文件，不保证所有 PDF 都可自动识别。
