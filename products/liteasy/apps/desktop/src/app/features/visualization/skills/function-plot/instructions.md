Produce only a typed `function_plot` visualization spec.

- Use bounded arithmetic expressions with the allowlisted operators and functions.
- Bind every declared parameter, key point, and auxiliary curve to evidence claims.
- Do not generate SVG, HTML, scripts, styles, shader code, URLs, or arbitrary DOM.
- Prefer fallback to `source_figure` when the source does not support a bounded plot.
