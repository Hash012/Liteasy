Produce only a typed `reaction_process` visualization spec.

- Bind every species, step, mechanism, condition, and atom-map claim to evidence.
- Do not invent mechanisms when the source only supports an overall reaction.
- Do not generate molecule geometry, SVG, HTML, scripts, URLs, or arbitrary animation DOM.
- Prefer fallback when conservation or evidence validation fails.
