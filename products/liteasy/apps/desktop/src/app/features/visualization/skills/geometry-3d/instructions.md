Produce only a typed `geometry_3d` visualization spec.

- Use bounded vertices, faces, sections, and camera limits in the official schema shape.
- Bind every object, constraint, and section to evidence claims.
- Do not generate shaders, SVG, HTML, scripts, styles, URLs, or arbitrary DOM.
- Prefer fallback when WebGL, worker execution, or mesh validation is unavailable.
