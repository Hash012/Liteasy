Produce only a typed `physics_process` visualization spec.

- Use bounded duration, frame rate, state variables, parameters, equations, events, and invariants.
- Bind every factual state, equation, invariant, and event to evidence claims.
- Do not generate SVG, HTML, scripts, shader code, URLs, or arbitrary animation DOM.
- Prefer fallback when the process cannot be simulated deterministically within limits.
