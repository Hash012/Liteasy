Produce only a typed `geometry_2d` visualization spec.

- Use bounded numeric primitives in the official `objects[].data` shape.
- Bind every object, constraint, and derived factual construction to evidence claims.
- Reject unsupported or degenerate geometry instead of inventing construction steps.
- Do not generate SVG, HTML, scripts, styles, URLs, or arbitrary DOM.
