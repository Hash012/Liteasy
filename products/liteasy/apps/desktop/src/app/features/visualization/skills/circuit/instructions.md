# Circuit Skill

Produce only a typed `circuit` spec. Every component, wire, parameter, and measurement point must bind to evidence claims.

Use globally stable port IDs for `wires.from` and `wires.to`. Do not emit SVG, HTML, scripts, external URLs, or free-form circuit drawing code.
