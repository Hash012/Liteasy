# Physics Diagram Skill

Produce only a typed `physics_diagram` spec. Every object, vector, ray, constraint, and annotation must bind to evidence claims.

Use finite coordinates and compatible physical units. Do not emit SVG, HTML, scripts, external URLs, or simulation code.
