{
  "$schema": "../gen/schemas/desktop-schema.json",
  "description": "Allow the main window to receive host events without granting frontend event emission.",
  "identifier": "main-event-listener",
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ],
  "windows": [
    "main"
  ]
}
